<%content_for :subnav do%>

  <div class='h3 bg-dark text-white px-3 py-5'>
    <%= turbo_frame_tag 'project_name' do%>
      <%= @project.name%>
    <%end%>
  </div>

  <nav class="navbar navbar-expand-lg bg-white border-bottom">
    <div class="container-fluid">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'General', project_path(id: @project.id), class: 'nav-link'%>
        </li>

        <li class="nav-item">
          <%= link_to 'Add Resources', project_resources_path(project_id: @project.id), class: 'nav-link'%>
        </li>

        <li class="nav-item">
          <%= link_to 'Explore', project_resources_path(project_id: @project.id), class: 'nav-link'%>
        </li>
        <li class="nav-item">
          <%= link_to playground_path, class: 'nav-link' do%>
            API 
            <i class="bi bi-arrow-up-right-square-fill"></i>

          <%end%>
        </li>        
      </ul>
    </div>
  </nav>



<%end%>